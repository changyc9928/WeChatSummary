# Backend (Java / Maven)
backend/target/
target/
backend/.mvn/
backend/.idea/
*.class

# Frontend (Node.js / Vite)
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/src/api/generated/

# Local build artifacts (in-workspace Go caches used by this repo's tooling)
.gocache/
.gotmp/
.gopath/
exporter/bridge.exe
exporter/bridge

# Distributable tool binaries (built locally, served by the backend)
tools/
tools/*.exe

raw-extract/
tmpzip/

# OS / IDE
.DS_Store
.idea/
*.iml

# Uploads / LLM output files
uploads/